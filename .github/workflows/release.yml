name: Publish to NPM

on:
  push:
    branches:
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Check if version is already published
        id: check
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          echo "version=$PACKAGE_VERSION" >> $GITHUB_OUTPUT

          PUBLISHED=$(npm view @jxtools/visualgit version 2>/dev/null || echo "none")
          if [ "$PUBLISHED" = "$PACKAGE_VERSION" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Version $PACKAGE_VERSION already published"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "Will publish $PACKAGE_VERSION (current: $PUBLISHED)"
          fi

      - name: Install dependencies
        if: steps.check.outputs.skip == 'false'
        run: npm ci

      - name: Run lint
        if: steps.check.outputs.skip == 'false'
        run: npm run lint

      - name: Run tests if configured
        if: steps.check.outputs.skip == 'false'
        run: |
          if node -e "const scripts = require('./package.json').scripts || {}; process.exit(scripts.test ? 0 : 1)"; then
            npm test
          else
            echo "No test script configured, skipping."
          fi

      - name: Build
        if: steps.check.outputs.skip == 'false'
        run: npm run build

      - name: Publish to NPM
        if: steps.check.outputs.skip == 'false'
        run: npm publish --provenance --access public
